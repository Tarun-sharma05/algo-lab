# OOP — Assignment

> Source: Kunal Kushwaha Java + DSA Bootcamp + additional problems
> Files go in: src/08_oop/  (use subfolders: classes_objects, constructors, keywords, inheritance, polymorphism, encapsulation, abstraction, interfaces)
> Rule: solve on onlinegdb.com first, then copy to .java file here and commit.

## Concepts to learn first
- Class, object, fields, methods
- Constructors: default, parameterized, copy constructor
- `this` and `super` keywords
- `static` and `final` keywords
- 4 pillars: Inheritance, Polymorphism, Encapsulation, Abstraction
- Method overloading vs method overriding
- Abstract class vs interface (when to use which)
- `instanceof` operator
- Object class methods: toString, equals, hashCode

## Problems

### Easy
- [ ] Student class — fields (name, age, grade) + constructor + getters/setters + toString override
- [ ] BankAccount class — deposit, withdraw, getBalance with validation
- [ ] Method overloading demo — same name, different parameter types/counts
- [ ] Method overriding demo — parent Animal + child Dog/Cat with speak()

### Medium
- [ ] Shape hierarchy — abstract Shape → Circle, Rectangle, Triangle each with area() and perimeter()
- [ ] Interface with multiple implementations — Drawable interface implemented by 3 classes
- [ ] Explain (code + comments): abstract class vs interface — when to use which
- [ ] Implement Comparable on a Student class to sort by grade

### Hard / Advanced
- [ ] Parking lot system (classes: ParkingLot, ParkingSlot, Vehicle)
- [ ] Simple LinkedList using OOP (Node class + LinkedList class)
- [ ] Library management system (Book, Member, Library classes with borrow/return logic)
